server:
  port: 5003

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      default-zone : http://localhost:5003
  server:
    wait-time-in-ms-when-sync-empty: 5

#Setting logging levels
#logging:
#  file:
#    path: /usr/local/pbeureka/logs
